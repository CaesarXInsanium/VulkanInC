(define-module (guile-graphics model)
               #:export (load-model
                          scale
                          rotate
                          translate))

(define (scale mat scalar)
  "DocString"
  (not-implemented! "scale"))

(define (rotate mat rot)
  "DocString"
  (not-implemented! "rotate"))

(define (translate mat trans)
  (not-implemented! "translate"))
